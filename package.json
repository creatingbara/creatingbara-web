{
  "name": "creatingbara-web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 execute creatingbara --local --file=schema.sql",
    "db:remote": "wrangler d1 execute creatingbara --remote --file=schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.108.0"
  }
}
